global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: "civiclens-backend"
    metrics_path: "/metrics"
    scrape_interval: 10s
    static_configs:
      - targets: ["backend:8000"]
        labels:
          app: "civiclens"
          environment: "production"
